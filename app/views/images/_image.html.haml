%div{id: dom_id(image)}
  %p
    %strong Name:
    = image.name

  %p
    %strong File:
    - if image.file.present?
      = image_tag image.file.url
    - else
      Изображение отсутствует

  %p
    %strong Ave value:
    = image.ave_value

  %p
    %strong Theme:
    = image.theme_id
