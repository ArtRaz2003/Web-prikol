.form_select
  = form_tag("/display_theme", remote: true, format: "js", data: { turbo: false }) do
    = select_tag :theme, options_for_select(@themes, selected: @selected_theme), prompt: 'Выберите тему', onchange: "$(this).parent('form').submit();", class: "form-control"